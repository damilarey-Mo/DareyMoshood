{
  {
  "version": 2,
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "npm": {
          "args": ["install", "--legacy-peer-deps"]
        }
      }
    }
  ]
}
